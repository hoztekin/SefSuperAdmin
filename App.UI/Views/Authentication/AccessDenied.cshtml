
@{
    ViewData["Title"] = "AccessDenied";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Bu Sayfaya eişme yetkiniz yoktur</h1>


@* <h1>Access Denied</h1>
<p>You don't have permission to access this resource.</p>

<h2>Authentication Information</h2>
<p>Is Authenticated: @ViewBag.IsAuthenticated</p>
<p>Authentication Type: @ViewBag.AuthType</p>
<p>Username: @ViewBag.Username</p>

<h3>Roles</h3>
<ul>
    @foreach (var role in ViewBag.Roles)
    {
        <li>@role</li>
    }
</ul>

<h3>Claims</h3>
<table class="table">
    <thead>
        <tr>
            <th>Type</th>
            <th>Value</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var claim in ViewBag.Claims)
        {
            <tr>
                <td>@claim.Type</td>
                <td>@claim.Value</td>
            </tr>
        }
    </tbody>
</table> *@

